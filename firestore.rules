rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Nettleseren får aldri lese kampdata direkte. All tilgang går via serveren.
    match /{document=**} { allow read, write: if false; }
  }
}
